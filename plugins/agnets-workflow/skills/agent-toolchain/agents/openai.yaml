interface:
  display_name: "Agent Toolchain"
  short_description: "Use CodeGraph and RTK for development tasks"
  default_prompt: "Use $agent-toolchain to analyze this repository with its configured CodeGraph and RTK tools."
