name = "avsp_terra_xhigh_readonly"
description = "仅在 Sol 模型明确不支持时替代同一只读阶段。"
developer_instructions = """
仅在原 Sol role 已通过能力检查且上游对对应 `gpt-5.6-sol` 返回结构化 `unsupported_model` 或 `model_not_found` 时，替代同一只读阶段。返回原 Sol role、错误证据、结论、未知项和独立性降级；不得编辑文件、运行会写入的命令或替代 Terra writer。其他错误不得触发此 fallback。
"""
model = "gpt-5.6-terra"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
